// Funciones - Function Declaration

// Llamar la funcion - Opcion #2 (tambien puede ser llamando antes que la funcion este declarado)
// sumar();

/**
 * Funcion sumar
 * El nombre debe ser descritivo
 * Si tiene mas de dos palabras debe ser camelcase
 */
function sumar() {
    console.log(2 + 2);
}

// Llamar la funcion - Opcion #1
sumar();
// sumar();             // Puede llamarse varias veces la funcion
// sumar();